# Seat - 智能座位编排系统

一个功能完善的拖拽式座位编排 Web 应用，支持学生信息管理、座位区域配置、拖拽排座、多方案管理和多格式导出等功能。

## ✨ 核心功能

### 📋 学生信息管理
- **多格式导入**：支持 Excel (.xlsx/.xls) 和 CSV 文件导入学生信息
- **智能字段映射**：自动识别姓名、学号、性别、身高、成绩等字段
- **学生列表管理**：查看待排座学生、编辑学生信息、删除学生
- **学生卡片**：支持拖拽操作，实时显示排座状态

### 🪑 座位编排
- **灵活配置**：支持 1-20 行/列的自定义座位布局
- **拖拽排座**：直观的拖拽操作，支持学生卡片拖拽到座位
- **座位移动**：支持座位间学生移动和交换
- **座位锁定**：可锁定特定座位，防止被修改
- **讲台显示**：可选显示讲台位置

### 🎲 智能排座
- **随机排座**：一键随机分配待排座学生到空座位
- **智能排座**：支持按身高、成绩、性别等条件智能排座
- **升序/降序**：支持升序或降序排列

### 👥 分组管理
- **创建分组**：支持创建多个学生分组
- **分组颜色**：每个分组可设置不同颜色标识
- **拖拽分组**：支持拖拽学生到分组
- **分组导出**：支持分组信息导出

### 💾 方案管理
- **多方案保存**：支持保存多个座位编排方案
- **方案切换**：快速切换不同方案
- **数据持久化**：自动保存到 LocalStorage
- **方案克隆**：支持复制现有方案

### 📤 多格式导出
- **图片导出**：支持 PNG、JPG 格式
- **文档导出**：支持 PDF、Excel (.xlsx)、Word (.docx) 格式
- **自定义设置**：可设置导出质量、背景色、文件名等
- **讲台显示**：可选是否在导出文件中显示讲台

## 🛠️ 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 + TypeScript |
| 状态管理 | Pinia |
| 路由管理 | Vue Router |
| UI 组件库 | Element Plus |
| CSS 框架 | Tailwind CSS |
| 构建工具 | Vite |
| 文件解析 | xlsx、papaparse |
| 导出功能 | html2canvas、jspdf、docx、xlsx-js-style |

## 📁 项目结构

```
src/
├── assets/                    # 静态资源
│   └── main.css              # 全局样式
├── components/                # 组件目录
│   ├── common/               # 通用组件
│   │   └── Toolbar.vue       # 工具栏组件
│   ├── export/               # 导出相关组件
│   │   └── ExportPanel.vue   # 导出面板
│   ├── group/                # 分组相关组件
│   │   ├── GroupArea.vue     # 分组区域
│   │   ├── GroupCard.vue     # 分组卡片
│   │   └── GroupPanel.vue    # 分组面板
│   ├── scheme/               # 方案相关组件
│   │   ├── SchemeManager.vue # 方案管理器
│   │   └── SchemeSwitcher.vue# 方案切换器
│   ├── seat/                 # 座位相关组件
│   │   ├── SeatCell.vue      # 单个座位
│   │   ├── SeatConfig.vue    # 座位配置
│   │   └── SeatGrid.vue      # 座位网格
│   └── student/              # 学生相关组件
│       ├── StudentCard.vue   # 学生卡片
│       ├── StudentImport.vue # 学生导入
│       └── StudentList.vue   # 学生列表
├── composables/              # 组合式函数
│   ├── useArrange.ts         # 排座逻辑
│   └── useDragDrop.ts        # 拖拽逻辑
├── stores/                   # Pinia 状态管理
│   ├── config.ts             # 配置状态
│   ├── group.ts              # 分组状态
│   ├── scheme.ts             # 方案状态
│   ├── seat.ts               # 座位状态
│   └── student.ts            # 学生状态
├── types/                    # TypeScript 类型定义
│   ├── config.ts             # 配置类型
│   ├── group.ts              # 分组类型
│   ├── scheme.ts             # 方案类型
│   ├── seat.ts               # 座位类型
│   └── student.ts            # 学生类型
├── utils/                    # 工具函数
│   ├── exporter.ts           # 导出工具
│   ├── groupExporter.ts      # 分组导出
│   └── parser.ts             # 文件解析
├── views/                    # 页面视图
│   └── HomeView.vue          # 主页面
├── router/                   # 路由配置
│   └── index.ts
├── App.vue                   # 根组件
└── main.ts                   # 入口文件
```

## 📦 安装与运行

### 环境要求

- Node.js: `^20.19.0` 或 `>=22.12.0`
- pnpm (推荐) 或 npm

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

启动后访问 http://localhost:5173

### 生产构建

```bash
pnpm build
```

### 预览构建结果

```bash
pnpm preview
```

### 代码检查

```bash
# 运行所有 lint 检查
pnpm lint

# 格式化代码
pnpm format

# 类型检查
pnpm type-check
```

## 📖 使用指南

### 1. 导入学生信息

点击左侧「导入学生」按钮，支持以下格式：
- **Excel 文件** (.xlsx, .xls)
- **CSV 文件** (.csv)

文件格式示例：

| 姓名 | 学号 | 性别 | 身高 | 成绩 |
|------|------|------|------|------|
| 张三 | 001 | 男 | 175 | 85 |
| 李四 | 002 | 女 | 165 | 92 |

支持的列名映射：
- 姓名：`name`、`姓名`、`学生姓名`
- 学号：`studentId`、`学号`、`student_id`
- 性别：`gender`、`性别`（支持：男/male/m、女/female/f）
- 身高：`height`、`身高`
- 成绩：`score`、`成绩`、`分数`

### 2. 配置座位布局

在座位配置面板中设置：
- **行数**：1-20 行
- **列数**：1-20 列
- **讲台显示**：是否显示讲台

### 3. 排座操作

#### 拖拽排座
1. 从左侧「待排学生」列表拖拽学生卡片
2. 放置到目标座位上
3. 支持座位间移动和交换

#### 随机排座
点击工具栏「随机排座」按钮，系统将随机分配所有待排座学生到空座位。

#### 智能排座
点击工具栏「智能排座」按钮，选择排座规则：
- **按身高**：按身高升序/降序排列
- **按成绩**：按成绩升序/降序排列
- **按性别**：按性别分组排列

### 4. 分组管理

在右侧分组面板中：
- 创建新分组
- 拖拽学生到分组
- 设置分组颜色
- 导出分组信息

### 5. 方案管理

#### 保存方案
点击工具栏「保存方案」按钮，输入方案名称保存当前编排。

#### 切换方案
点击工具栏方案下拉菜单，选择已保存的方案快速切换。

### 6. 导出座位表

点击工具栏「导出」按钮，选择导出格式：
- **PNG**：高清图片格式
- **JPG**：压缩图片格式
- **PDF**：便携文档格式
- **Excel**：可编辑表格格式
- **Word**：可编辑文档格式

## ⚙️ 配置说明

### 座位配置

```typescript
interface SeatConfig {
  rows: number          // 行数 (1-20)
  cols: number          // 列数 (1-20)
  showPodium: boolean   // 是否显示讲台
  seatWidth: number     // 座位宽度 (px)
  seatHeight: number    // 座位高度 (px)
  gapX: number          // 水平间距 (px)
  gapY: number          // 垂直间距 (px)
}
```

### 导出配置

```typescript
interface ExportConfig {
  format: 'png' | 'jpg' | 'pdf' | 'excel' | 'word'
  quality: number           // 图片质量 (0-1)
  showSeatNumber: boolean   // 是否显示座位编号
  showStudentId: boolean    // 是否显示学号
  backgroundColor: string   // 背景颜色
}
```

## 🔧 开发指南

### IDE 推荐配置

- **编辑器**：[VS Code](https://code.visualstudio.com/)
- **Vue 插件**：[Vue - Official](https://marketplace.visualstudio.com/items?itemName=Vue.volar)（请禁用 Vetur）

### 浏览器开发工具

- **Chrome/Edge**：
  - [Vue.js devtools](https://chromewebstore.google.com/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd)
  - [开启 Custom Object Formatter](http://bit.ly/object-formatters)
- **Firefox**：
  - [Vue.js devtools](https://addons.mozilla.org/en-US/firefox/addon/vue-js-devtools/)
  - [开启 Custom Object Formatter](https://fxdx.dev/firefox-devtools-custom-object-formatters/)

### TypeScript 支持

项目使用 `vue-tsc` 进行类型检查，Volar 插件提供 `.vue` 文件的类型支持。

### 代码规范

项目使用以下工具保证代码质量：
- **ESLint**：代码检查
- **Oxlint**：高性能 lint 工具
- **Prettier**：代码格式化

## 📋 数据结构

### 学生数据

```typescript
interface Student {
  id: string                    // 唯一标识
  name: string                  // 姓名
  studentId?: string            // 学号
  gender?: 'male' | 'female'    // 性别
  height?: number               // 身高
  score?: number                // 成绩
  color?: string                // 标识颜色
}
```

### 座位数据

```typescript
interface Seat {
  id: string          // 座位ID
  row: number         // 行号
  col: number         // 列号
  studentId?: string  // 入座学生ID
  isLocked: boolean   // 是否锁定
}
```

### 分组数据

```typescript
interface StudentGroup {
  id: string            // 分组ID
  name: string          // 分组名称
  studentIds: string[]  // 学生ID列表
  color?: string        // 分组颜色
  createdAt: number     // 创建时间
  updatedAt: number     // 更新时间
}
```

## 🙋 常见问题

### Q: 支持哪些文件格式导入？

A: 目前支持 Excel (.xlsx, .xls) 和 CSV 文件格式。

### Q: 数据会自动保存吗？

A: 是的，所有数据会自动保存到浏览器的 LocalStorage 中，刷新页面不会丢失。

### Q: 如何备份我的座位方案？

A: 目前数据保存在浏览器本地，建议定期导出 Excel 或 Word 文件作为备份。

### Q: 支持多少学生同时编排？

A: 理论上支持 100+ 学生流畅操作，拖拽响应时间 < 100ms。

### Q: 导出的文件可以编辑吗？

A: Excel 和 Word 格式的导出文件可以在对应软件中编辑，图片和 PDF 格式为只读。

### Q: 如何在移动设备上使用？

A: 系统支持响应式布局，但拖拽功能在桌面设备上体验更佳。

## 📄 License

本项目采用 MIT 许可证。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
